import {Box, Typography} from "@mui/material";

export default function Home() {
    // `session` will match the returned value of `callbacks.session()` from `NextAuth()`

    return (
        <Box>
            <Typography>Working thingggg</Typography>
        </Box>
    )
}
